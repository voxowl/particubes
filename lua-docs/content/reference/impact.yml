keywords: ["particubes", "game", "mobile", "scripting", "cube", "voxel", "world"]
type: "Impact"
description: An `Impact` object can be returned when casting a ray. (see [Player.CastRay](/reference/Player#CastRay)
and PointerEvent.CastRay. All properties are read-only.

constructors: 
  - description: "Created using CastRay functions. local impact = Player.CastRay"

properties:
    - name: "Block"
      type: "Block"
      description: "Block touched by the ray. Not `nil` if the [Impact](/reference/Impact) represents a [Block](/reference/Block)."
      code:  |
        local impact = Local.Player:CastRay()
        if impact.Block ~= nil then
	        print(impact.Block.Id)	-- prints hit block's id
        end
    - name: "Distance"
      type: "Number"
      description: "Distance to impact when casting a ray. (see [Player.CastRay](/reference/Player#CastRay))
    - name: "FaceTouched"
      type: "Number"
      description: "Not `nil` if the [Impact](/reference/Impact) represents a [Block](/reference/Block).
        Index of hit block's face."
    - name: "Player"
      type: "Player"
      description: "Player touched by the ray."
